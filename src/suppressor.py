"""Layer 4b — False-positive suppression rules.

Placeholder scaffold (Step 1). Implemented in Step 5.
"""
from __future__ import annotations

import pandas as pd


def apply_suppression(df: pd.DataFrame, profiles: pd.DataFrame) -> pd.DataFrame:
    """Apply all suppression rules, adding suppressed and suppression_reason columns."""
    raise NotImplementedError
